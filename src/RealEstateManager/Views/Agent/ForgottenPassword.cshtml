@using RealEstateManager.Utils
@model RealEstateManager.Models.Agent.AgentForgottenPasswordModel

@{
    ViewBag.Title = Localization.GetString("AgentForgottenPasswordPage_Title");
}

@Html.ValidationSummary(true, null, new { @class = "text-danger" })

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="card">
        <div class="card-header bg-primary text-white">
            <h3>@Localization.GetString("AgentForgottenPasswordForm_Header")</h3>
        </div>
        <div class="card-body bg-light">
            @Localization.GetString("AgentForgottenPasswordForm_RecoveryDescription")
            <hr/>
            <div class="form-group">
                @Html.LabelFor(x => x.EmailAddress)
                @Html.EditorFor(x => x.EmailAddress, new {htmlAttributes = new {@class = "form-control"}})
                @Html.ValidationMessageFor(x => x.EmailAddress, null, new {@class = "text-danger"})
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">@Localization.GetString("AgentForgottenPasswordForm_SubmitButtonText")</button>
            </div>
        </div>
    </div>
}
